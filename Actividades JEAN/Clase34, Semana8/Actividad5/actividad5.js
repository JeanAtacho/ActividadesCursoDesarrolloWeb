function Array() {
    var nombres = new Array();
    var notas = new Array();
    var total = 0;

    for (var x = 0; x < 5; x++) {

        nombres[x] = prompt("Introduce el nombre de la posición" + x)

        notas[x] = parseInt(prompt("Introduce la nota de"))

        total = total + notas^[x];

    }





}

alert(total);